# tl-bitrix-public-screenshots

Public hosting for screenshots referenced from `InitechSoftware/tl-bitrix-integration` issues. The integration repo is private, so raw GitHub URLs there can't be embedded inline. This repo holds public copies for that purpose only.
